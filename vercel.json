{
  "version": 2,
  "builds": [
    {
      "src": "BOS-master/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "BOS-master/dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
